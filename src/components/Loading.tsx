import React from 'react'
import { Typography } from '@/components/ui/typography'

const Loading: React.FC = () => {
  return <Typography variant="normal">Loading...</Typography>
}

export default Loading
